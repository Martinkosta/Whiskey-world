const authController = require('./auth');
const whiskeyController = require('./whiskeyController');
const postController = require('./postController');

module.exports = {
    authController,
    whiskeyController,
    postController,
}